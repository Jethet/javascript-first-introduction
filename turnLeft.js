let rover = {
    direction: "N"
};

let directions = "";

function turnLeft(rover){
    if (rover.direction === "N" && directions === "l"){
            rover.direction === "W";
            console.log(rover.direction);
  }   
}
