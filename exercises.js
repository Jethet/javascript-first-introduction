/* Assign the value 7 to variable a. Assign the value of 7 to variable b.
Assign the contents of a to variable b. */

var myList;
myList = "thirty-five"
var i;
//myList = ["one", "two", "three", "four", "five", "fifteen", "thirty-five", "one hundred and ten"]//

/* ALWAYS FIRST DECLARE A VARIABLE
AND THEN ASSIGN A VARIABLE */

function charCount() {
  for (let i = 0; i++) {
  console.log(i.length());
  }
}

charCount(myList)
